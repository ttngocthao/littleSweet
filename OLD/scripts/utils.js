/**
 * Test a value to see if it exists.
 * @param theItem 
 * @return true if the value is defined and is not null.
 */
function isValid(theItem) {
    return theItem != undefined && theItem != null;
}
